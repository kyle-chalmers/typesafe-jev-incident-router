# TypeSafe Jev Incident Router

A confidence-gated Python router for synthetic data incidents. Exact registry facts win first. TypeSafe Jev supplies bounded semantic judgments when the registry cannot answer, and Python decides whether to assign a response team or preserve the evidence for human review.

## Status

The project structure is being built test-first. Complete setup and usage instructions will be added with the runnable commands.

